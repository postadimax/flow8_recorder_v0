name: flow8_recorder
description: Recording app for Behringer Flow 8
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # Versione 4.4.4: ultra-stabile e senza i bug della 5.x
  record: 4.4.4
  path_provider: ^2.1.2
  permission_handler: ^11.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
